name: Cost of Living Content Generation

on:
  schedule:
    # Monday and Thursday at 08:00 UTC — 2 articles per week from Sanity queue
    - cron: '0 8 * * 1,4'
  workflow_dispatch:
    inputs:
      mode:
        description: 'Generation mode'
        required: false
        default: 'queue'
        type: choice
        options:
          - 'queue'
          - 'manual'
      count:
        description: 'Number of articles to generate from queue (queue mode only)'
        required: false
        default: '1'
        type: string
      city:
        description: 'City name (manual mode only, e.g., London)'
        required: false
        type: string
      country:
        description: 'Country name (manual mode only, e.g., UK)'
        required: false
        type: string
      year:
        description: 'Year (manual mode only)'
        required: false
        default: '2026'
        type: string
      comparison_city:
        description: 'Comparison city (manual mode, optional)'
        required: false
        type: string
      article_mode:
        description: 'Article mode (manual mode only)'
        required: false
        default: 'city'
        type: choice
        options:
          - 'city'
          - 'comparison'
          - 'budget'
      dry_run:
        description: 'Dry run (preview only, no articles created)'
        required: false
        default: false
        type: boolean

env:
  NODE_VERSION: '20'

jobs:
  generate-cost-of-living:
    name: Generate Cost of Living Article
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Debug - Show workflow info
        run: |
          echo "🔍 Workflow Debug Info:"
          echo "   Event: ${{ github.event_name }}"
          echo "   Ref: ${{ github.ref }}"
          echo "   SHA: ${{ github.sha }}"
          echo "   Workflow: ${{ github.workflow }}"
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "   Mode: ${{ github.event.inputs.mode }}"
            echo "   Count: ${{ github.event.inputs.count }}"
            echo "   City: ${{ github.event.inputs.city }}"
            echo "   Country: ${{ github.event.inputs.country }}"
            echo "   Dry Run: ${{ github.event.inputs.dry_run }}"
          fi

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Mask secrets in logs
        run: |
          echo "::add-mask::${{ secrets.GEMINI_API_KEY }}"
          echo "::add-mask::${{ secrets.SANITY_API_TOKEN }}"
          echo "::add-mask::${{ secrets.UNSPLASH_ACCESS_KEY }}"
          echo "::add-mask::${{ secrets.YOUTUBE_API_KEY }}"

      - name: Verify secrets are configured
        run: |
          if [ -z "${{ secrets.GCP_PROJECT_ID }}" ] && [ -z "${{ secrets.GEMINI_API_KEY }}" ]; then
            echo "❌ Either GCP_PROJECT_ID (for Vertex AI) or GEMINI_API_KEY (for Google AI Studio) must be configured!"
            exit 1
          fi
          if [ -z "${{ secrets.SANITY_API_TOKEN }}" ]; then
            echo "❌ SANITY_API_TOKEN not configured in GitHub Secrets!"
            exit 1
          fi
          if [ -n "${{ secrets.GCP_PROJECT_ID }}" ]; then
            echo "✅ Vertex AI configured (GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }})"
          else
            echo "✅ Google AI Studio API configured"
          fi
          echo "✅ All required secrets are configured"

      - name: Setup Vertex AI credentials
        env:
          GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
          GOOGLE_APPLICATION_CREDENTIALS_JSON: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS_JSON }}
        run: |
          if [ -z "$GCP_PROJECT_ID" ]; then
            echo "ℹ️ GCP_PROJECT_ID not set, skipping Vertex AI credentials setup"
            exit 0
          fi

          if [ -z "$GOOGLE_APPLICATION_CREDENTIALS_JSON" ]; then
            echo "❌ GOOGLE_APPLICATION_CREDENTIALS_JSON not set - Vertex AI requires credentials"
            exit 1
          fi

          CREDS_FILE="${{ runner.temp }}/gcp-credentials.json"
          echo "$GOOGLE_APPLICATION_CREDENTIALS_JSON" > "$CREDS_FILE"

          if [ ! -f "$CREDS_FILE" ]; then
            echo "❌ Failed to create credentials file"
            exit 1
          fi

          if command -v jq > /dev/null 2>&1; then
            if ! jq empty "$CREDS_FILE" > /dev/null 2>&1; then
              echo "❌ Credentials file is not valid JSON"
              exit 1
            fi
            echo "✅ JSON validation passed (using jq)"
          else
            echo "⚠️ jq not available, skipping JSON validation"
          fi

          echo "GOOGLE_APPLICATION_CREDENTIALS=$CREDS_FILE" >> $GITHUB_ENV
          echo "✅ Vertex AI credentials file created at $CREDS_FILE"

          chmod 600 "$CREDS_FILE"

      - name: Verify credentials before generation
        env:
          GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
          GCP_LOCATION: ${{ secrets.GCP_LOCATION }}
        run: |
          if [ -z "$GCP_PROJECT_ID" ]; then
            echo "ℹ️ GCP_PROJECT_ID not set, skipping credentials verification"
            exit 0
          fi

          if [ -z "$GOOGLE_APPLICATION_CREDENTIALS" ]; then
            echo "❌ GOOGLE_APPLICATION_CREDENTIALS environment variable not set"
            exit 1
          fi
          if [ ! -f "$GOOGLE_APPLICATION_CREDENTIALS" ]; then
            echo "❌ Credentials file not found: $GOOGLE_APPLICATION_CREDENTIALS"
            exit 1
          fi
          echo "✅ Credentials file verified: $GOOGLE_APPLICATION_CREDENTIALS"
          echo "✅ GCP_PROJECT_ID: $GCP_PROJECT_ID"
          echo "✅ GCP_LOCATION: ${GCP_LOCATION:-us-central1}"

      - name: Ensure Cost of Living pillar exists
        env:
          SANITY_API_TOKEN: ${{ secrets.SANITY_API_TOKEN }}
          NEXT_PUBLIC_SANITY_PROJECT_ID: ${{ secrets.NEXT_PUBLIC_SANITY_PROJECT_ID }}
          NEXT_PUBLIC_SANITY_DATASET: production
        run: |
          echo "🏛️ Checking if Cost of Living pillar page exists..."
          node scripts/create-costofliving-pillar.js || echo "⚠️ Pillar creation script completed (may already exist)"

      - name: Generate Cost of Living Articles
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
          GCP_LOCATION: ${{ secrets.GCP_LOCATION || 'us-central1' }}
          GOOGLE_APPLICATION_CREDENTIALS: ${{ env.GOOGLE_APPLICATION_CREDENTIALS }}
          GEMINI_MODEL: ${{ secrets.GEMINI_MODEL || 'gemini-2.5-pro' }}
          SANITY_API_TOKEN: ${{ secrets.SANITY_API_TOKEN }}
          NEXT_PUBLIC_SANITY_PROJECT_ID: ${{ secrets.NEXT_PUBLIC_SANITY_PROJECT_ID }}
          NEXT_PUBLIC_SANITY_DATASET: production
          UNSPLASH_ACCESS_KEY: ${{ secrets.UNSPLASH_ACCESS_KEY }}
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
          STRICT_DEDUP: "1"
        run: |
          GEN_MODE="${{ github.event.inputs.mode || 'queue' }}"
          DRY_RUN="${{ github.event.inputs.dry_run || 'false' }}"

          if [ "${{ github.event_name }}" = "schedule" ]; then
            GEN_MODE="queue"
          fi

          if [ "$GEN_MODE" = "manual" ]; then
            CITY="${{ github.event.inputs.city }}"
            COUNTRY="${{ github.event.inputs.country }}"
            YEAR="${{ github.event.inputs.year || '2026' }}"
            COMPARISON_CITY="${{ github.event.inputs.comparison_city }}"
            MODE="${{ github.event.inputs.article_mode || 'city' }}"

            if [ -z "$CITY" ] || [ -z "$COUNTRY" ]; then
              echo "❌ City and Country are required in manual mode!"
              exit 1
            fi

            echo "🏙️ Manual generation:"
            echo "   City: $CITY"
            echo "   Country: $COUNTRY"
            echo "   Year: $YEAR"
            echo "   Mode: $MODE"

            ARGS=("$CITY" "$COUNTRY" "$YEAR")
            if [ -n "$COMPARISON_CITY" ] && [ "$COMPARISON_CITY" != "" ]; then
              ARGS+=("$COMPARISON_CITY")
              [ -n "$MODE" ] && ARGS+=("$MODE")
            elif [ -n "$MODE" ] && [ "$MODE" != "" ]; then
              ARGS+=("" "$MODE")
            fi

            if [ "$DRY_RUN" = "true" ]; then
              echo "🔍 DRY RUN MODE"
              DRY_RUN=1 node scripts/ai-costofliving-generator.js "${ARGS[@]}" || echo "✅ Dry run completed"
            else
              node scripts/ai-costofliving-generator.js "${ARGS[@]}"
            fi
          else
            # Queue mode — process from Sanity topic queue
            COUNT="${{ github.event.inputs.count || '1' }}"
            COUNT="${COUNT:-1}"

            echo "📋 Queue mode — generating $COUNT article(s) from Sanity topic queue..."

            if [ "$DRY_RUN" = "true" ]; then
              node scripts/generate-col-batch.js --count "$COUNT" --dry-run
            else
              node scripts/generate-col-batch.js --count "$COUNT"
            fi
          fi
